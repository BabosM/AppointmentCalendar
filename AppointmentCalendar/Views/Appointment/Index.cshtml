<div class="row">
    <div class="container">
        @if (User.IsInRole(AppointmentCalendar.Utility.Helper.Admin))
        {
            <div class="form-group col-2" id="divDoctorList">
                <label> Doctor list</label>
                @Html.DropDownList("doctorId", new SelectList(ViewBag.DoctorList, "Id", "Name"), new { @class = "form-control select2" })
            </div>
        }
        <div id="calendar"></div>
    </div>
</div>

<partial name="_AddEditAppointment" />